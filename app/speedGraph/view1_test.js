'use strict';

describe('myApp.speedGraph module', function() {

  beforeEach(module('myApp.speedGraph'));

  describe('speedGraph controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});